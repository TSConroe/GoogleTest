package base.utils;

public class Constants {
    public static final String BROWSER = System.getenv("BROWSER");
    public static final String DOMAIN = System.getenv("DOMAIN");

    public static final int HEIGHT = Integer.parseInt(System.getenv("HEIGHT"));
    public static final int WIDTH = Integer.parseInt(System.getenv("WIDTH"));
}
